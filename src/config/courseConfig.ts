/**
 * Course layout configuration.
 * Re-exports the course-related values from gameConfig for convenience.
 * Edit gameConfig.ts to change course positions.
 */

export {
  LOBBY_SPAWN,
  START_PAD_POSITION,
  START_PAD_SIZE,
  FINISH_GATE_POSITION,
  FINISH_GATE_SIZE,
  CHECKPOINT_POSITIONS,
  CHECKPOINT_SIZE,
  OUT_OF_BOUNDS_Y,
  COURSE_ID,
} from './gameConfig';
